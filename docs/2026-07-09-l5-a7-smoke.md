# L5 Governance — A7 Live Smoke (spec §15.11)

**Date:** 2026-07-09 ~01:30 EEST · **Host:** headless gateway (`feral-cli gateway start`, 127.0.0.1:11435) · **Sidecar:** dist build 2026-07-09 01:26 (A1–A5 + fixes below)

## Sequence (§15.11) — all via `feral governance …` / `feral meta …`

| Step | Command | Result |
|---|---|---|
| baseline | `governance status` | `source=file failClosed=false policyId=gp-1 frozen=none` (genesis bootstrapped on first boot) |
| propose-tighten | `governance propose gp-2.json` (confidenceMin 0.95→0.96, episodeMaxIterations 1000→500) | `✓ direction=tightening status=active` → **auto-adopted**, no approval (G-INV-2) |
| reject | `governance reject gp-2 -m …` (an earlier queued proposal) | `✓ reject ok`, removed from pending |
| freeze | `governance freeze l6 -m "A7 smoke"` | `✓`, status shows `frozen l6` |
| refusal end-to-end | `feral meta evolve` | `✗ frozen by governance (frozen.l6) — see feral governance status` (CLI → API → sidecar → MetaEvolution, AC6) |
| unfreeze | `governance unfreeze l6` | `✓`, frozen=none |
| rollback | `governance rollback` | `✓` — parent document re-activated under new id (`gp-4`, parentId `gp-3`), one step (AC9) |
| verify | `governance verify` | chains **green** (history=9 audit=9 approvals=0); journal: 6 prior days green, **today's file red** (see below); exit 1 as required by fail-loud |
| doctor | `feral doctor` | governance check red for the same journal reason — correct |

## Known-good failure: today's journal file

`journal-2026-07-08.jsonl row=227 unchained row after the chain started` — this file was written by BOTH sidecar generations on upgrade day (the pre-A2 live sidecar wrote unchained rows interleaved with the new chained writer). Indistinguishable from tampering by design, so verify fails loud and `defaultReadWindow` excludes the file from L6 fitness (G-INV-4). Self-heals at UTC midnight when a fresh day file starts; every prior day verifies green. Re-run `feral governance verify` after 2026-07-09 00:00 UTC for a full-green transcript.

## Bugs found BY this smoke (all fixed + tested)

1. **approve read policyId from `msg.id`** — the roundtrip correlation UUID — so every CLI/API approve failed with `unknown proposal: <uuid>` (fix: 08e3627).
2. **propose body flattened over the envelope** — the sidecar handler reads `msg.document`; the API route now wraps the posted policy as `{document: …}`.
3. **genesis started the 24h auto-adopt cooldown** — genesis is written with actor `system` but is not an auto-adoption (§5); every fresh install's first tightening proposal queued for 24h. `cooldownActive()` now skips the genesis row (parentId null). Regression test added.
4. **`governance verify` printed `✗ unknown error` on failure** — the generic `!ok` guard ate the payload before the verify renderer; a failed verify now prints the per-chain/per-file report and exits 1.

## Operational notes

- Sidecar single-writer lock (`~/.feral/agent/.writer.lock`) is NOT released when the sidecar is killed or the gateway is stopped — the next sidecar spawn crash-loops on it (`gateway stop` → start needs a manual `Remove-Item`). Pre-existing rough edge, worth a follow-up (stale-lock detection by pid).
- `target/debug/feral.exe` is the DESKTOP app; running it headless rewrites `~/.feral/api-token` and 401s a running gateway. The CLI is `feral-cli.exe`.
